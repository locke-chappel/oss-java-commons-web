/* test/custom.js */
var Vars = {
    timeout : #context.timeout#,
    timeout_enabled : #context.timeout.enabled#,
    pathResource : "#context.path.resource#",
    pathurl : "#context.path.url#",
    cookie_prefix : "#context.cookies.prefix#",
    resolved : "#id#",
    not_resolved : "#junk#",
    timeout_not : "#context.timeout'#",
    loggin_prefix : "#context.logging.prefix#"
};
